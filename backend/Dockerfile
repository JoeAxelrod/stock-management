# ---- Runtime image (small, self-contained) ----
FROM node:20-alpine

# Create app directory
WORKDIR /app

# 1️⃣ Copy backend package manifest and install **production** dependencies
COPY package*.json ./
RUN npm install --omit=dev && npm cache clean --force

# 2️⃣ Copy compiled output generated by `nx build backend`
COPY dist/ ./dist

# 3️⃣ Expose API port and launch
EXPOSE 3000
ENV NODE_ENV=production
CMD ["node", "dist/main.js"]